{
  "hooks": {
    "SessionStart": [{
      "matcher": "*",
      "hooks": [{
        "type": "command",
        "command": "${CLAUDE_PLUGIN_ROOT}/hooks/ensure-claude-md-gates.sh",
        "timeout": 5
      }, {
        "type": "prompt",
        "prompt": "Twining MCP tools are available for persistent project coordination. CLAUDE.md contains the authoritative \"Twining Lifecycle Gates\" section — follow those three gates (assemble, decide, verify) for every task. Start with `twining_assemble` before reading code or making changes."
      }]
    }],
    "SubagentStop": [{
      "matcher": "*",
      "hooks": [{
        "type": "command",
        "command": "${CLAUDE_PLUGIN_ROOT}/hooks/subagent-stop-hook.sh",
        "timeout": 10
      }]
    }],
    "Stop": [{
      "matcher": "*",
      "hooks": [{
        "type": "command",
        "command": "${CLAUDE_PLUGIN_ROOT}/hooks/stop-hook.sh",
        "timeout": 10
      }]
    }]
  }
}
